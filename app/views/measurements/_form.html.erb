<%= form_with(model: measurement, local: true) do |form| %>
  <% if measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

      <ul>
      <% measurement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight %>
  </div>

  <div class="field">
    <%= form.label :fat_percentage %>
    <%= form.text_field :fat_percentage %>
  </div>

  <div class="field">
    <%= form.label :measured_on %>
    <%= form.text_field :measured_on %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
